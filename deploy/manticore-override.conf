# manticore.service の systemd ドロップイン（仕様書13.1）。
# 配置先: /etc/systemd/system/manticore.service.d/override.conf
#         → systemctl daemon-reload
#
# パッケージ既定の RestartSec は 100ms で、起動に失敗し続ける状況（インデックス破損・
# ディスク満杯など）では毎秒10回の再起動ループになり、ログとCPUを食い潰す。
# 仕様書のとおり10秒空けて、復旧の余地を残す。
[Service]
RestartSec=10
